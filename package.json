{
  "dependencies": {
    "@tailwindcss/cli": "^4.1.7",
    "tailwindcss": "^4.1.7"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
  "server": "json-server --watch db.json --port 3000",
  "nak":"npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch"
}

}
